rule tune:
    input:
        x_train = "results/split_data/x_train.csv.zip",
        y_train = "results/split_data/y_train.csv.zip"
    output:
        trained_model = "results/{model}/{classifier}/tuned_parameters.yml"
    params:
        classifier = "{classifier}",
        model = "{model}",
        random_state = config["random_state"],
        config_path = config["workdir"],
        config = "{classifier}",
    conda:
        "../envs/hydra.yml"
    threads:
        config["threads"]
    shell:
        """
        python scripts/tune.py hydra/launcher=submitit_slurm --cfg hydra -p hydra.launcher
        # python scripts/tune.py \
        # --config-path {params.config_path}/configs \
        # --config-name {params.config} \
        # hydra/sweeper=optuna --cfg hydra -p hydra.sweeper
        # --n-jobs {threads} \
        # --x-train {input.x_train} \
        # --y-train {input.y_train} \
        # --trained-model {output.trained_model} \
        # --classifier {params.classifier} \
        # --model {params.model} \
        # --random-state {params.random_state}
        """
