defaults:
  - _self_
  - hydra

hydra:
  sweeper:
    params:
      classifier: choice("random_forest")
      n_jobs: choice(1)
      n_estimators: choice(100, 1000)
      criterion: choice("gini", "entropy", "log_loss")
      x_train: choice(None)
      y_train: choice(None)

classifier: 1
n_jobs: 1
n_estimators: 1
criterion: 1
x_train: 1
y_train: 1
